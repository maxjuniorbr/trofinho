# EAS Build ignore — based on .gitignore but includes google-services.json
# (needed by Android prebuild, excluded from git for security)

# Dependencies
node_modules/

# Build outputs
.expo/
dist/
coverage/

# Generated types
expo-env.d.ts
*.tsbuildinfo

# Debug logs
npm-debug.*
yarn-debug.*
yarn-error.*

# Native builds (generated by `expo run:android` / `expo run:ios`)
/ios/
/android/

# Environment variables
.env.*
!.env.example

# OS
.DS_Store

# IDE / local tooling
.idea/
.vscode/
*.code-workspace

# AI assistants — local config, not shared
.agents/
.kiro/
.github/copilot-instructions.md
.github/instructions/
.github/prompts/

# Supabase
supabase/.branches/
supabase/.temp/

# Firebase service account keys (sensitive — obtain from Firebase console)
*-firebase-adminsdk-*.json
# NOTE: google-services.json is intentionally NOT ignored here (needed for Android build)

# Certificates
*.pem

# Local workspace
temp/

# Skills lock (generated by skills CLI)
skills-lock.json
