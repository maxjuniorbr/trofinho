# Exclude SQL migration files from copy-paste detection.
# Migrations are append-only DDL files with intentionally repeated patterns
# (GRANT, CREATE POLICY, RLS boilerplate). They cannot be refactored.
sonar.cpd.exclusions=supabase/migrations/**
